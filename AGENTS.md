# MULO Frontend Agent Instructions

이 문서는 `mulo-fe` 저장소에서 작업하는 AI 코딩 에이전트의 필수 규칙이다.

## 1. 작업 전 확인 순서

1. 현재 요청과 관련된 `RULE.md`, `README.md`, `docs/` 문서를 읽는다.
2. 변경 대상과 기존 구현을 먼저 확인한다.
3. API 작업은 최신 OpenAPI와 `docs/api-integration.md`를 함께 확인한다.
4. 충돌하거나 불명확한 규칙은 임의로 해석하지 않고 개발자에게 확인한다.

우선순위는 현재 개발자 요청, `RULE.md`, 이 문서, 기존 프로젝트 관례, 일반 프레임워크 관례 순서다.

## 2. 기술 기준

- Node.js 24
- npm
- Vite
- React
- TypeScript `strict`
- ESLint
- Prettier

승인 없이 Router, HTTP 클라이언트, 상태 관리, UI 프레임워크, 테스트 프레임워크 또는 대체 빌드 도구를 추가하지 않는다.

## 3. 의존 방향

```text
app -> pages -> features -> entities -> shared
```

- 하위 계층은 상위 계층을 import하지 않는다.
- `shared`는 다른 프로젝트 계층에 의존하지 않는다.
- `pages`는 기능을 조립하며 비즈니스 로직을 직접 구현하지 않는다.
- 기능 전용 코드를 예상만으로 `shared`에 배치하지 않는다.
- 새 디렉터리를 만들기 전 `docs/architecture.md`에서 책임과 위치를 확인한다.

## 4. 변경 권한

기존 구조 안의 컴포넌트, Hook, 타입, 스타일, 문서 및 검증 코드는 요청 범위에서 수정할 수 있다.

다음 변경은 먼저 개발자의 승인을 받는다.

- 의존성 추가·삭제·업그레이드
- 인증·인가·토큰 저장 방식 변경
- 공용 API 클라이언트와 공용 계층 변경
- 공개 API 계약 또는 환경변수 이름 변경
- 디렉터리 의존 방향 변경
- 빌드·배포·CI 범위 변경
- 요청과 무관한 광범위한 리팩터링

## 5. TypeScript와 코드 작성

- `strict`를 유지한다.
- `any`를 사용하지 않는다. 외부 값은 `unknown`으로 받고 검증 후 좁힌다.
- API 요청·응답과 컴포넌트 props는 명시적인 타입으로 표현한다.
- 함수, Hook, 서비스 메서드에는 책임과 역할을 설명하는 짧은 주석을 작성한다.
- 이벤트 처리처럼 이름과 문맥만으로 책임이 명확한 인라인 콜백에는 중복 주석을 남기지 않는다.
- 주석은 구현 과정을 반복하지 않고 코드만으로 알기 어려운 책임과 제약을 설명한다.
- 사용되지 않는 추상화, 범용화, 빈 디렉터리를 미리 만들지 않는다.

## 6. API와 보안

- 최신 OpenAPI를 경로, HTTP method, 필드, 상태 코드의 기준으로 사용한다.
- `VITE_` 환경변수에는 브라우저에 공개해도 되는 값만 둔다.
- Secret, Access Token, Refresh Token을 소스나 `.env` 예제에 넣지 않는다.
- Refresh Token을 JavaScript 저장소나 상태에서 읽거나 저장하지 않는다.
- 인증과 CSRF 세부 규칙은 `docs/api-integration.md`를 따른다.
- 계약이 문서와 구현에서 다르면 임의로 한쪽을 맞추지 말고 차이를 보고한다.

## 7. 검증

변경 범위에 맞춰 작은 검사부터 실행하고, 완료 전 기본 검사를 수행한다.

```bash
npm run prettier
npm run lint
npm run typecheck
```

빌드나 애플리케이션 코드에 영향을 주는 변경에는 다음도 실행한다.

```bash
npm run build
```

- 실패 검사를 삭제하거나 규칙을 약화해 통과시키지 않는다.
- 실행하지 않은 검증을 성공했다고 보고하지 않는다.
- 실행하지 못한 검증은 명령과 이유를 완료 보고에 남긴다.

## 8. 변경 설명과 완료 보고

- 코드 설명에는 실제 파일명과 코드 줄 번호를 포함한다.
- 수정 제안에는 기존 내용과 수정 내용을 함께 제시하고 이유를 설명한다.
- 의견과 기술 선택에는 공식 문서 또는 프로젝트 문서를 근거로 제시한다.
- 완료 보고에는 변경 내용, 변경 파일, 검증 결과, 의존성·API·환경변수 영향, 남은 위험을 포함한다.

## 9. 금지 사항

- `main`, `develop`에서 직접 개발하거나 push하지 않는다.
- 승인 없이 의존성이나 공용 구조를 변경하지 않는다.
- Secret 또는 실제 `.env`를 커밋하지 않는다.
- `any`, 타입 검사 비활성화, 무분별한 타입 단언으로 오류를 숨기지 않는다.
- 관련 없는 파일을 수정하거나 전체 저장소를 재포맷하지 않는다.
- 검증 없이 완료를 선언하지 않는다.
